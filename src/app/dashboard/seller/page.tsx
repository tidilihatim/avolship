import React from 'react'

type Props = {}

export const metadata = {
  title: 'Overview | AvolShip',
}

const page = async (props: Props) => {
  return (
    <div>page</div>
  )
}

export default page